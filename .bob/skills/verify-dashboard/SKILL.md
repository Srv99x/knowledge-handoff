---
name: verify-dashboard
description: Verify the Knowledge Continuity Suite dashboard renders all six views from live reports, rebuild the single-file bundle, and restart the local server on port 8765. Use whenever dashboard/app.js, dashboard/styles.css, dashboard/index.html, or backend/outputs/*.json change and the user wants confirmation the dashboard still works.
---

# Verify + rebuild + redeploy the dashboard

Working directory: ~/ibm-hackathon/knowledge-handoff-4

CONSTRAINTS (never break these):
- Read-only on data and backend. DO NOT modify anything under backend/agents/, backend/outputs/, or sample-repos/. The dashboard only presents the JSON reports.
- Zero-dependency: no npm, no installs; build uses python3 stdlib only.
- NEVER hand-edit dashboard/dashboard.html — it is GENERATED by dashboard/build.py from index.html + styles.css + app.js + the six reports.

STEPS:
1. Run `bash dashboard/verify.sh` (one tool call).
2. Inspect its output. It checks: app.js syntax; renders every view (overview, risk, busfactor, complexity, documentation, onboarding, extraction, pipeline) from the real reports via Node and asserts required markers (e.g. onboarding shows "direct 0 commits", extraction shows per-draft "why"); rebuilds dashboard.html; restarts the server on :8765; health-checks GET / and /api/all == 200; spot-checks the headline facts against the reports.
3. Report PASS/FAIL per section, plus the headline facts it printed (73 tracked files · 8 HIGH/38 MEDIUM/18 LOW of 64 ranked · 8 onboarding files · 24 successors · 8 drafts).
4. On FAIL: show the failing section + affected dashboard/ files. Never auto-fix backend.
5. On PASS: remind the user to hard-refresh (Cmd+Shift+R).
