# AGENTS.md

This file provides guidance to agents when working with code in this repository.

## Documentation Context (Non-Obvious Only)

- **`README.md` describes the hackathon template, not the Knowledge Continuity Suite** — it is boilerplate from the IBM hackathon starter and does not reflect actual project design decisions.
- **`agents/` and `outputs/` are empty** — there is no existing agent logic or output format to reference.
- **`sample-repos/` contains external repos cloned for testing** (e.g. `steam-snap`) — any docs or code inside are from those upstream projects, not this one.
- **`.env.example` cannot be read by Bob** (blocked by `.bobignore`) — to understand expected environment variables, ask the user or check the template structure manually.
- **The project has a single git commit** ("Initial commit") — there is no change history or prior design to infer intent from.
